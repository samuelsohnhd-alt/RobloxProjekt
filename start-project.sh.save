#!/bin/bash
# DEPRECATED: Dieses Skript ist veraltet, nicht erforderlich für reine Rojo/VS/GitHub-Arbeitsabläufe.
# Wenn du das Projekt bereinigen möchtest, führe: ./cleanup.sh --report   (zum Prüfen)
# oder ./cleanup.sh (zum interaktiven Löschen)
echo "Dieses Skript ist veraltet. Siehe ./cleanup.sh --report"
exit 0
cd ~/RobloxProjekt || exit 1

# 1. Rojo-Server im Hintergrund starten
./serve.sh &

# 2. Build-Datei erzeugen
rojo build -o game.rbxlx

# 3. Datei mit Studio öffnen
open game.rbxlx

echo "✅ RB7 Projekt gestartet! Änderungen syncen jetzt zwischen Studio ↔ src/ ↔ GitHub"



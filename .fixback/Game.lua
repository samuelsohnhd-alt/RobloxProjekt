--!strict
-- Zentrale Spiel-Config (nur Konstanten, kein Hardcode sonstwo)
local GameConfig = {
	ROUND_TIME_DEFAULT = 300, -- Sekunden
	TARGET_SCORE       = 25,
	RESPAWN_TIME       = 5,
	FRIENDLY_FIRE      = false,
}
return GameConfig
